name: Strictacode

on:
  push:
    branches:
      - '[0-9]+.[0-9]+.x'
  pull_request:
    branches:
      - '[0-9]+.[0-9]+.x'

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/pyproject.toml') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - uses: qarium/strictacode/.github/actions/analyze@0.0.x
        with:
          install-cmd: pip install strictacode
          working-directory: goga_tool_viewer
        env:
          STRICTACODE_SCORE: 20
          STRICTACODE_RP: 20
          STRICTACODE_OP: 10
          STRICTACODE_IMB: 15
          STRICTACODE_DENSITY: 20
          STRICTACODE_RP_DIFF: 5
          STRICTACODE_OP_DIFF: 5
          STRICTACODE_DENSITY_DIFF: 5
          STRICTACODE_SCORE_DIFF: 3
