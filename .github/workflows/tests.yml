name: Tests
on:
  push:
    branches:
      - '[0-9]+.[0-9]+.x'
  pull_request:
    branches:
      - '[0-9]+.[0-9]+.x'

jobs:
  test:
    uses: qarium/ci/.github/workflows/library-tests.yml@0.0.x
    with:
      python-versions: '["3.10", "3.11", "3.12", "3.13", "3.14"]'
